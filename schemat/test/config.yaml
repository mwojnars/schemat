# NOT USED right now because cluster configuration CANNOT be overridden in a non-bootstrap ring "ring-test".

# ID of the Cluster that this node is, or should be, a part of
cluster: 1014

bootstrap_rings:
  - name: boot_kernel
    file: ./schemat/data/00_kernel.data.yaml                    # core categories; practically immutable (very rarely changed)
  - name: boot_cluster
    file: ./schemat/data/01_cluster.data.1032.yaml
